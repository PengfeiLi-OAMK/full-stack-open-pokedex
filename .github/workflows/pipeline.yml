name: Deployment Pipeline

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
jobs:
    simple_deplotment_pipeline:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: '16'
            - name: Install Dependencies
              run: npm install
            - name: check style
              run: npm run eslint
            - name: build
              run: npm run build
            - name: test
              run: npm test